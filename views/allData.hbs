<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1" />
  <title>{{title}}</title>
  <style>
    /* Main container with padding and max-width */
    .container {
      max-width: 1200px;
      margin: 20px auto;
      padding: 0 15px;
      box-sizing: border-box;
    }

    /* Scroll wrapper for horizontal scroll */
    .table-container {
      width: 100%;
      overflow-x: auto; /* enables horizontal scroll if needed */
      -webkit-overflow-scrolling: touch; /* smooth scroll on mobile */
      border: 1px solid #ccc;
      padding: 10px 0;
    }

    /* Table styles */
    table {
      width: 100%;
      border-collapse: collapse;
      min-width: 900px; /* ensures table is wide enough to avoid squeezing */
      font-family: Arial, sans-serif;
    }

    th, td {
      border: 1px solid #ddd;
      padding: 8px 12px;
      text-align: left;
      vertical-align: top;
      white-space: nowrap; /* keep content in one line */
    }

    th {
      background-color: #f4f4f4;
      font-weight: bold;
    }

    /* Zebra stripes */
    tbody tr:nth-child(odd) {
      background-color: #fafafa;
    }
  </style>
</head>
<body>
  <div class="container">
    <h2>{{title}}</h2>

    <div class="table-container">
      <table>
        <thead>
          <tr>
            <th>Movie ID</th>
            <th>Title</th>
            <th>Year</th>
            <th>Genre</th>
            <th>Director</th>
            <th>Actors</th>
            <th>Plot</th>
          </tr>
        </thead>
        <tbody>
          {{#each movies}}
          <tr>
            <td>{{Movie_ID}}</td>
            <td>{{Title}}</td>
            <td>{{Year}}</td>
            <td>{{Genre}}</td>
            <td>{{Director}}</td>
            <td>{{Actors}}</td>
            <td>{{Plot}}</td>
          </tr>
          {{/each}}
        </tbody>
      </table>
    </div>
  </div>
</body>
</html>
